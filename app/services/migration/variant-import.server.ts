import { resolveShopifyId, saveMapping } from "../external-api/mapper.server";

export async function importVariantsToShopify(shop: string, session: any, variants: any[]) {
  // Add variants to products created in importProductsToShopify
}
