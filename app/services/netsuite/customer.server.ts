import { NetSuiteClient } from "./client.server";

export async function syncNetSuiteCustomerToShopify(shop: string, netsuiteCustomerId: string) {
  // Sync NetSuite -> Shopify
}

export async function syncShopifyCustomerToNetSuite(shop: string, shopifyCustomerId: string) {
  // Sync Shopify -> NetSuite
}
