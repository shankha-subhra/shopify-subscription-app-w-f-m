import db from "../../db.server";

export async function mapNetSuiteFieldsToShopify(shop: string, entityType: string, nsData: any) {
  // Logic to translate custom netsuite fields to shopify equivalents
  return {};
}
